import 'package:flutter/material.dart';

class CacheTab extends StatelessWidget {
  const CacheTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Cache Music Kosong"),
    );
  }
}
